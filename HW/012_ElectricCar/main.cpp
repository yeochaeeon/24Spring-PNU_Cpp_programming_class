#include <iostream>

int main(){

    const double capacity ,km_per_kW, distance;
    double charging_rate ;
    
    std::cin >> capacity >> km_per_kW >> charging_rate >> distance ;
    const double km_per_kW = capacity / 100 * efficiency ;
    double now = 0;
    while (now < distance){

    }



}